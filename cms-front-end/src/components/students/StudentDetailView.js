import './StudentDetailView.css';

const StudentDetailView = (props) => {
    return (
        <div>
            <p>THis is student id : {props.id} details page</p>
        </div>);
}

export default StudentDetailView;